# Partie 4 — Base de données vectorielle & Embeddings

## Contenu
- `BD_vectorielle_embeddings(final).ipynb` : classification d'articles (sport vs cuisine) via embeddings `all-MiniLM-L6-v2` et index FAISS.
- `article_sport.docx`, `article_cuisine.docx`, `article_test.docx` : articles de test (.docx).
- `mon_index.faiss` : index FAISS sérialisé (généré à l'exécution).

## Dépendances
- `sentence-transformers`, `faiss-cpu`, `python-docx` (installations dans le notebook)
- Les fichiers `.docx` doivent être dans le même dossier que le notebook.

## Utilisation de l'IA
Voir le mini rapport intégré en fin de notebook (section "Mini rapport").
